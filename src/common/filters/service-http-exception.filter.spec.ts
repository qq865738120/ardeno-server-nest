import { ServiceHttpExceptionFilter } from './service-http-exception.filter';

describe('ServiceHttpExceptionFilter', () => {
  it('should be defined', () => {
    expect(new ServiceHttpExceptionFilter()).toBeDefined();
  });
});
